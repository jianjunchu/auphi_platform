<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aofei.dataservice.mapper.ReceiveInterfaceMapper">



    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
          a.SERVICE_ID AS serviceId
        , a.SERVICE_NAME AS serviceName
        , a.SERVICE_IDENTIFY AS serviceIdentify
        , a.SERVICE_URL AS serviceUrl
        , a.DATASOURCE AS datasource
        , a.TABLENAME AS tablename
        , a.CREATEDATE AS createdate
        , a.INTERFACE_DESC AS interfaceDesc
        , a.ID_DATABASE AS databaseId
        , b.NAME AS databaseName
        , a.CREATE_USER AS createUser
        , a.UPDATE_USER AS updateUser
        , a.CREATE_TIME AS createTime
        , a.UPDATE_TIME AS updateTime
        , a.DEL_FLAG AS delFlag
    </sql>

    <sql id="Base_Joins">
        LEFT JOIN R_DATABASE b ON b.ID_DATABASE = a.ID_DATABASE
    </sql>

    <select id="findList"  resultType="com.aofei.dataservice.entity.ReceiveInterface">
        select
        <include refid="Base_Column_List"/>
        from DATASERVICE_INTERFACE a
        <include refid="Base_Joins"/>
        <where>
            a.DEL_FLAG = 0
        </where>
    </select>
</mapper>