<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aofei.dataservice.mapper.ServiceMonitorMapper">



    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
          a.MONITOR_ID AS monitorId
        , a.SERVICE_ID AS serviceId
        , a.START_TIME AS startTime
        , a.END_TIME AS endTime
        , a.STATUS AS status
        , a.USERNAME AS userName
        , a.SYSTEM_NAME AS systemName
        , a.CREATE_USER AS createUser
        , a.UPDATE_USER AS updateUser
        , a.CREATE_TIME AS createTime
        , a.UPDATE_TIME AS updateTime
        , a.DEL_FLAG AS delFlag
        , dsi.SERVICE_NAME AS serviceName
    </sql>

    <sql id="Base_Joins">
        LEFT JOIN DATASERVICE_INTERFACE dsi ON dsi.SERVICE_ID = a.SERVICE_ID
    </sql>

    <select id="findList"  resultType="com.aofei.dataservice.entity.ServiceAuth">
        select
        <include refid="Base_Column_List"/>
        from DATASERVICE_MONITOR a
        <include refid="Base_Joins"/>
        <where>
            a.DEL_FLAG = 0
        </where>
    </select>

</mapper>
